// Centralized API base for frontend
// In production (Netlify), set this to your Render backend URL.
// For local dev, you can comment this out to fall back to localhost in JS files.

window.API_BASE = 'https://traffic-violation-detection-system.onrender.com';
